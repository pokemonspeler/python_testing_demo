name: Python Testing

on:
  pull_request:
    paths:
      - app/**
      - test/**
      - config/**
      - .pylint.rc
  push:
    branch:
      - master

jobs:
  testing:
    uses: ./.github/workflows/actions/python/testing.yml

  linting:
    uses: ./.github/workflows/actions/python/linting.yml